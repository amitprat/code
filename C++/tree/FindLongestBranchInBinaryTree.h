#include "../header.h"

class FindLongestBranch {
   public:
    struct Node {
        int val;
        Node *left, *right;
        Node(int val) : val(val), left(nullptr), right(nullptr) {}
    };
    static void test() {
        Node* root;
    }
};